import { NotesSliderPipe } from './notes-slider.pipe';

describe('NotesSliderPipe', () => {
  it('create an instance', () => {
    const pipe = new NotesSliderPipe();
    expect(pipe).toBeTruthy();
  });
});
